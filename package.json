{
  "name": "meccha-webar-hide-and-seek",
  "version": "1.0.0",
  "description": "WebAR multiplayer hide & seek (camouflage) — Node.js, Express, Socket.io, Three.js, WebXR",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "certs": "mkdir -p certs && openssl req -x509 -newkey rsa:2048 -nodes -keyout certs/key.pem -out certs/cert.pem -days 365 -subj '/CN=localhost'",
    "test": "node test/flow.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "socket.io-client": "^4.7.5",
    "three": "^0.160.0"
  },
  "license": "MIT"
}
